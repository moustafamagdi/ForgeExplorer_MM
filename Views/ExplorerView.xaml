<UserControl x:Class="ForgeExplorer.Views.ExplorerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:behaviors="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:viewmodels="clr-namespace:ForgeExplorer.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="410" d:DesignWidth="800">

    <UserControl.DataContext>
        <viewmodels:ExplorerViewModel />
    </UserControl.DataContext>

    <behaviors:Interaction.Triggers>
        <behaviors:EventTrigger EventName="Loaded">
            <behaviors:InvokeCommandAction Command="{Binding ReloadCommand}" />
        </behaviors:EventTrigger>
    </behaviors:Interaction.Triggers>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.50*" />
            <ColumnDefinition Width="0.50*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Column="0" Width="218" Height="28" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="8,10,0,0" Content="Directory Tree" />
        <TreeView Grid.Column="0" VerticalAlignment="Stretch" Margin="8,38,13,42" ItemsSource="{Binding Items}">
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}" DataType="{x:Type viewmodels:ItemViewModel}">
                    <TextBlock Text="{Binding Name}" />
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                    <Setter Property="FontWeight" Value="Normal" />
                    <Style.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="FontWeight" Value="Bold" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </TreeView.ItemContainerStyle>
        </TreeView>

        <Label Grid.Column="1" Width="218" Height="28" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="4,10,0,0" Content="Directory Listing" />
        <ListView Grid.Column="1" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="4,38,8,42" ItemsSource="{Binding Content}">
            <ListView.ItemTemplate>
                <DataTemplate DataType="{x:Type viewmodels:ItemViewModel}">
                    <TextBlock Text="{Binding Name}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Label Grid.Column="0" Width="218" Height="28" HorizontalAlignment="Left" VerticalAlignment="Bottom" Margin="8,0,0,8" Content="{Binding LogLabel}" />
        <Button Grid.Column="0" Height="23" Width="75" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,13,8" Content="Reload" Command="{Binding ReloadCommand}" />
        <Button Grid.Column="1" Height="23" Width="75" HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,8,8" Content="Close" Command="{Binding CloseCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" />
    </Grid>
</UserControl>
